import 'jest-localstorage-mock';
import { EncryptStorageOptions, EncryptStorage } from '..';
interface makeSutParams extends EncryptStorageOptions {
    secretKey?: string;
    noOptions?: boolean;
}
export declare const makeSut: (params?: makeSutParams) => EncryptStorage;
export declare const test1: () => void;
export {};
