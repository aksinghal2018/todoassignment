export declare type EncAlgorithm = 'AES' | 'Rabbit' | 'RC4' | 'RC4Drop';
export declare type Encryptation = {
    encrypt(value: string): string;
    decrypt(value: string): string;
};
export declare function getEncriptation(encAlgorithm: EncAlgorithm, secretKey: string): Encryptation;
